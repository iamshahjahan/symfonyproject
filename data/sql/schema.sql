CREATE TABLE user_details (id BIGINT AUTO_INCREMENT, user_name VARCHAR(255), user_password VARCHAR(255) NOT NULL, is_activated TINYINT(1) DEFAULT '0' NOT NULL, user_email VARCHAR(255) NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
